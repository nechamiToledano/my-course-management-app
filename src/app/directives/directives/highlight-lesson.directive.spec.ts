import { HighlightLessonDirective } from './highlight-lesson.directive';

describe('HighlightLessonDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightLessonDirective();
    expect(directive).toBeTruthy();
  });
});
